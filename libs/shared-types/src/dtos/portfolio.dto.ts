export interface PortfolioDto {
  // Placeholder DTO contract for future implementation.
  id: string;
  userId: string;
  name: string;
  description?: string;
  createdAt: string;
  updatedAt: string;
}
